version: 2.1

workflows:
  nightly:
    jobs:
      - build:
          name: build-2.1.4-buster
          airflow_version: 2.1.4
          distribution_name: buster
          dev_build: true
          extra_args:
            --build-arg VERSION=$(curl https://pip.astronomer.io/simple/astronomer-certified/latest-2.1.4.build)
          image_name: "ap-airflow:2.1.4-buster"
      - scan-trivy:
          name: scan-trivy-2.1.4-buster-onbuild
          airflow_version: 2.1.4
          distribution: buster
          distribution_name: buster-onbuild
          image_name: "ap-airflow:2.1.4-buster"
          requires:
            - build-2.1.4-buster
      - test:
          name: test-2.1.4-buster-images
          tag: "2.1.4-buster"
          requires:
            - build-2.1.4-buster
      - push:
          name: push-2.1.4-buster
          dev_build: true
          tag: "2.1.4-buster"
          extra_tags: "2.1.4-buster-${CIRCLE_BUILD_NUM}"
          context:
            - quay.io
            - docker.io
          requires:
            - scan-trivy-2.1.4-buster-onbuild
            - test-2.1.4-buster-images
          filters:
            branches:
              only:
                - master
      - push:
          name: push-2.1.4-buster-onbuild
          dev_build: true
          tag: "2.1.4-buster-onbuild"
          extra_tags: "2.1.4-buster-onbuild-${CIRCLE_BUILD_NUM},2.1.4-4.dev-buster-onbuild"
          context:
            - quay.io
            - docker.io
          requires:
            - scan-trivy-2.1.4-buster-onbuild
            - test-2.1.4-buster-images
          filters:
            branches:
              only:
                - master
      - build:
          name: build-2.2.0-bullseye
          airflow_version: 2.2.0
          distribution_name: bullseye
          dev_build: true
          extra_args:
            --build-arg VERSION=$(curl https://pip.astronomer.io/simple/astronomer-certified/latest-2.2.0.build)
          image_name: "ap-airflow:2.2.0"
      - scan-trivy:
          name: scan-trivy-2.2.0-bullseye-onbuild
          airflow_version: 2.2.0
          distribution: bullseye
          distribution_name: bullseye-onbuild
          image_name: "ap-airflow:2.2.0"
          requires:
            - build-2.2.0-bullseye
      - test:
          name: test-2.2.0-bullseye-images
          tag: "2.2.0"
          requires:
            - build-2.2.0-bullseye
      - push:
          name: push-2.2.0-bullseye
          dev_build: true
          tag: "2.2.0"
          extra_tags: "2.2.0-${CIRCLE_BUILD_NUM}"
          context:
            - quay.io
            - docker.io
          requires:
            - scan-trivy-2.2.0-bullseye-onbuild
            - test-2.2.0-bullseye-images
          filters:
            branches:
              only:
                - master
      - push:
          name: push-2.2.0-bullseye-onbuild
          dev_build: true
          tag: "2.2.0-onbuild"
          extra_tags: "2.2.0-onbuild-${CIRCLE_BUILD_NUM},2.2.0-5.dev-onbuild"
          context:
            - quay.io
            - docker.io
          requires:
            - scan-trivy-2.2.0-bullseye-onbuild
            - test-2.2.0-bullseye-images
          filters:
            branches:
              only:
                - master
      - build:
          name: build-2.2.0-buster
          airflow_version: 2.2.0
          distribution_name: buster
          dev_build: true
          extra_args:
            --build-arg VERSION=$(curl https://pip.astronomer.io/simple/astronomer-certified/latest-2.2.0.build)
          image_name: "ap-airflow:2.2.0-buster"
      - scan-trivy:
          name: scan-trivy-2.2.0-buster-onbuild
          airflow_version: 2.2.0
          distribution: buster
          distribution_name: buster-onbuild
          image_name: "ap-airflow:2.2.0-buster"
          requires:
            - build-2.2.0-buster
      - test:
          name: test-2.2.0-buster-images
          tag: "2.2.0-buster"
          requires:
            - build-2.2.0-buster
      - push:
          name: push-2.2.0-buster
          dev_build: true
          tag: "2.2.0-buster"
          extra_tags: "2.2.0-buster-${CIRCLE_BUILD_NUM}"
          context:
            - quay.io
            - docker.io
          requires:
            - scan-trivy-2.2.0-buster-onbuild
            - test-2.2.0-buster-images
          filters:
            branches:
              only:
                - master
      - push:
          name: push-2.2.0-buster-onbuild
          dev_build: true
          tag: "2.2.0-buster-onbuild"
          extra_tags: "2.2.0-buster-onbuild-${CIRCLE_BUILD_NUM},2.2.0-5.dev-buster-onbuild"
          context:
            - quay.io
            - docker.io
          requires:
            - scan-trivy-2.2.0-buster-onbuild
            - test-2.2.0-buster-images
          filters:
            branches:
              only:
                - master
      - build:
          name: build-2.2.1-bullseye
          airflow_version: 2.2.1
          distribution_name: bullseye
          dev_build: true
          extra_args:
            --build-arg VERSION=$(curl https://pip.astronomer.io/simple/astronomer-certified/latest-2.2.1.build)
          image_name: "ap-airflow:2.2.1"
      - scan-trivy:
          name: scan-trivy-2.2.1-bullseye-onbuild
          airflow_version: 2.2.1
          distribution: bullseye
          distribution_name: bullseye-onbuild
          image_name: "ap-airflow:2.2.1"
          requires:
            - build-2.2.1-bullseye
      - test:
          name: test-2.2.1-bullseye-images
          tag: "2.2.1"
          requires:
            - build-2.2.1-bullseye
      - push:
          name: push-2.2.1-bullseye
          dev_build: true
          tag: "2.2.1"
          extra_tags: "2.2.1-${CIRCLE_BUILD_NUM}"
          context:
            - quay.io
            - docker.io
          requires:
            - scan-trivy-2.2.1-bullseye-onbuild
            - test-2.2.1-bullseye-images
          filters:
            branches:
              only:
                - master
      - push:
          name: push-2.2.1-bullseye-onbuild
          dev_build: true
          tag: "2.2.1-onbuild"
          extra_tags: "2.2.1-onbuild-${CIRCLE_BUILD_NUM},2.2.1-3.dev-onbuild"
          context:
            - quay.io
            - docker.io
          requires:
            - scan-trivy-2.2.1-bullseye-onbuild
            - test-2.2.1-bullseye-images
          filters:
            branches:
              only:
                - master
      - build:
          name: build-2.2.1-buster
          airflow_version: 2.2.1
          distribution_name: buster
          dev_build: true
          extra_args:
            --build-arg VERSION=$(curl https://pip.astronomer.io/simple/astronomer-certified/latest-2.2.1.build)
          image_name: "ap-airflow:2.2.1-buster"
      - scan-trivy:
          name: scan-trivy-2.2.1-buster-onbuild
          airflow_version: 2.2.1
          distribution: buster
          distribution_name: buster-onbuild
          image_name: "ap-airflow:2.2.1-buster"
          requires:
            - build-2.2.1-buster
      - test:
          name: test-2.2.1-buster-images
          tag: "2.2.1-buster"
          requires:
            - build-2.2.1-buster
      - push:
          name: push-2.2.1-buster
          dev_build: true
          tag: "2.2.1-buster"
          extra_tags: "2.2.1-buster-${CIRCLE_BUILD_NUM}"
          context:
            - quay.io
            - docker.io
          requires:
            - scan-trivy-2.2.1-buster-onbuild
            - test-2.2.1-buster-images
          filters:
            branches:
              only:
                - master
      - push:
          name: push-2.2.1-buster-onbuild
          dev_build: true
          tag: "2.2.1-buster-onbuild"
          extra_tags: "2.2.1-buster-onbuild-${CIRCLE_BUILD_NUM},2.2.1-3.dev-buster-onbuild"
          context:
            - quay.io
            - docker.io
          requires:
            - scan-trivy-2.2.1-buster-onbuild
            - test-2.2.1-buster-images
          filters:
            branches:
              only:
                - master
      - build:
          name: build-2.2.2-bullseye
          airflow_version: 2.2.2
          distribution_name: bullseye
          dev_build: true
          extra_args:
            --build-arg VERSION=$(curl https://pip.astronomer.io/simple/astronomer-certified/latest-2.2.2.build)
          image_name: "ap-airflow:2.2.2"
      - scan-trivy:
          name: scan-trivy-2.2.2-bullseye-onbuild
          airflow_version: 2.2.2
          distribution: bullseye
          distribution_name: bullseye-onbuild
          image_name: "ap-airflow:2.2.2"
          requires:
            - build-2.2.2-bullseye
      - test:
          name: test-2.2.2-bullseye-images
          tag: "2.2.2"
          requires:
            - build-2.2.2-bullseye
      - push:
          name: push-2.2.2-bullseye
          dev_build: true
          tag: "2.2.2"
          extra_tags: "2.2.2-${CIRCLE_BUILD_NUM}"
          context:
            - quay.io
            - docker.io
          requires:
            - scan-trivy-2.2.2-bullseye-onbuild
            - test-2.2.2-bullseye-images
          filters:
            branches:
              only:
                - master
      - push:
          name: push-2.2.2-bullseye-onbuild
          dev_build: true
          tag: "2.2.2-onbuild"
          extra_tags: "2.2.2-onbuild-${CIRCLE_BUILD_NUM},2.2.2-2.dev-onbuild"
          context:
            - quay.io
            - docker.io
          requires:
            - scan-trivy-2.2.2-bullseye-onbuild
            - test-2.2.2-bullseye-images
          filters:
            branches:
              only:
                - master
      - build:
          name: build-2.2.2-buster
          airflow_version: 2.2.2
          distribution_name: buster
          dev_build: true
          extra_args:
            --build-arg VERSION=$(curl https://pip.astronomer.io/simple/astronomer-certified/latest-2.2.2.build)
          image_name: "ap-airflow:2.2.2-buster"
      - scan-trivy:
          name: scan-trivy-2.2.2-buster-onbuild
          airflow_version: 2.2.2
          distribution: buster
          distribution_name: buster-onbuild
          image_name: "ap-airflow:2.2.2-buster"
          requires:
            - build-2.2.2-buster
      - test:
          name: test-2.2.2-buster-images
          tag: "2.2.2-buster"
          requires:
            - build-2.2.2-buster
      - push:
          name: push-2.2.2-buster
          dev_build: true
          tag: "2.2.2-buster"
          extra_tags: "2.2.2-buster-${CIRCLE_BUILD_NUM}"
          context:
            - quay.io
            - docker.io
          requires:
            - scan-trivy-2.2.2-buster-onbuild
            - test-2.2.2-buster-images
          filters:
            branches:
              only:
                - master
      - push:
          name: push-2.2.2-buster-onbuild
          dev_build: true
          tag: "2.2.2-buster-onbuild"
          extra_tags: "2.2.2-buster-onbuild-${CIRCLE_BUILD_NUM},2.2.2-2.dev-buster-onbuild"
          context:
            - quay.io
            - docker.io
          requires:
            - scan-trivy-2.2.2-buster-onbuild
            - test-2.2.2-buster-images
          filters:
            branches:
              only:
                - master
      - download-file:
          name: download-latest-main-build-info
          url: https://pip.astronomer.io/simple/astronomer-certified/latest-main.build.json
          file: latest-main.build.json
          output_file: extra-tags-main.txt
      - build:
          name: build-main-bullseye
          airflow_version: main
          distribution_name: bullseye
          dev_build: true
          extra_args:
            --build-arg VERSION=$(curl https://pip.astronomer.io/simple/astronomer-certified/latest-main.build)
            --label org.apache.airflow.ci.build.date=$(jq -r '.date' </tmp/workspace/latest-main.build.json)
            --label org.apache.airflow.ci.build.url=$(jq -r '.github.run.url' </tmp/workspace/latest-main.build.json)
            --label org.apache.airflow.ci.build.version=$(jq -r '.output.airflow.package.version' </tmp/workspace/latest-main.build.json)
            --label org.apache.airflow.ci.js.node.version_string=$(jq -r '.js.node.version' </tmp/workspace/latest-main.build.json)
            --label org.apache.airflow.ci.js.npm.version_string=$(jq -r '.js.npm.version' </tmp/workspace/latest-main.build.json)
            --label org.apache.airflow.ci.js.yarn.version_string=$(jq -r '.js.yarn.version' </tmp/workspace/latest-main.build.json)
            --label org.apache.airflow.ci.python.version_string="$(jq -r '.python.version' </tmp/workspace/latest-main.build.json)"
            --label org.apache.airflow.git.commit_sha=$(jq -r '.git.commit' </tmp/workspace/latest-main.build.json)
            --label org.apache.airflow.git.$(jq -r '.git.ref.type' </tmp/workspace/latest-main.build.json)=$(jq -r '.git.ref.name' </tmp/workspace/latest-main.build.json)
            --label io.astronomer.astronomer_certified.build.version=$(jq -r '.output.astronomer_certified.package.version' </tmp/workspace/latest-main.build.json)
          image_name: "ap-airflow:main"
          requires:
            - download-latest-main-build-info
      - scan-trivy:
          name: scan-trivy-main-bullseye-onbuild
          airflow_version: main
          distribution: bullseye
          distribution_name: bullseye-onbuild
          image_name: "ap-airflow:main"
          requires:
            - build-main-bullseye
      - test:
          name: test-main-bullseye-images
          tag: "main"
          requires:
            - build-main-bullseye
      - push:
          name: push-main-bullseye
          dev_build: true
          tag: "main"
          extra_tags: "main-${CIRCLE_BUILD_NUM}"
          extra_tags_file: /tmp/workspace/extra-tags-main.txt
          context:
            - quay.io
            - docker.io
          requires:
            - scan-trivy-main-bullseye-onbuild
            - test-main-bullseye-images
          filters:
            branches:
              only:
                - master
      - push:
          name: push-main-bullseye-onbuild
          dev_build: true
          tag: "main-onbuild"
          extra_tags: "main-onbuild-${CIRCLE_BUILD_NUM},main.dev-onbuild"
          context:
            - quay.io
            - docker.io
          requires:
            - scan-trivy-main-bullseye-onbuild
            - test-main-bullseye-images
          filters:
            branches:
              only:
                - master

jobs:
  rebase-astro-main:
    machine:
      image: ubuntu-2004:202107-02
    steps:
      - add_ssh_keys:
          fingerprints:
            - "11:0b:15:6b:f2:77:3e:93:c5:83:0e:8a:0a:93:2e:7e"
      - run:
          name: Rebase astronomer/airflow:astro-main onto main
          command: |
            export GIT_SSH_COMMAND="ssh -o IdentitiesOnly=yes -i $HOME/.ssh/id_rsa_110b156bf2773e93c5830e8a0a932e7e"
            git clone git@github.com:astronomer/airflow.git
            cd airflow
            git config user.name "astronomer/ap-airflow GitHub:CircleCI integration"
            git config user.email "astronomer@users.noreply.github.com"
            git branch --verbose
            git fetch --all
            git checkout astro-main
            git --no-pager log --max-count 4
            git rebase main
            git --no-pager log --max-count 4
            git push --force origin astro-main
            git --no-pager remote --verbose
  static-checks:
    executor: machine-executor
    description: Static Checks
    steps:
      - checkout
      - run:
          name: Load archived Docker image
          command: |
            pyenv global 3.8.5
            pip install -r .circleci/test-requirements.txt
            pre-commit run --all-files || { git --no-pager diff && false ; }
  download-file:
    executor: machine-executor
    description: Download a file to disk
    parameters:
      url:
        description: The full URL to download from
        type: string
      file:
        description: The output file to write to (existing files will be overwritten)
        type: string
      output_file:
        description: The file to write
        type: string
    steps:
      - run:
          name: Downloading file with curl
          command: |
            curl << parameters.url >> > << parameters.file >>
            # Avoid polluting logs with a huge file - <= 100 lines or <= 2048 bytes
            if [[ "$(wc -l << parameters.file >>)" -le 100 || "$(wc -c)" -le 2048 ]]; then
              echo "Output file:"
              cat << parameters.file >>
            else
              echo "Emitting output file would pollute logs; skipping"
            fi
      - run:
          name: Writing extra steps file
          command: |
            echo "Input JSON file:"
            cat << parameters.file >>
            jq --raw-output '.output.astronomer_certified.package.version' < << parameters.file >> > << parameters.output_file >>
            echo "Output newline-separated file:"
            cat << parameters.output_file >>
      - persist_to_workspace:
          root: .
          paths:
            - << parameters.output_file >>
            - << parameters.file >>
  build:
    executor: docker-executor
    description: Build Airflow images
    parameters:
      airflow_version:
        description: "The Airflow version, for example '1.10.5'"
        type: string
      distribution_name:
        description: "The base distribution of the container"
        type: string
      dev_build:
        description: "Indicate if this is a dev build"
        type: boolean
      extra_args:
        description: "Extra args to pass to pass to Docker build command"
        default: ""
        type: string
      image_name:
        type: string
        default: "ap-airflow:2.2.0"
    steps:
      - docker-build-base-and-onbuild:
          airflow_version: "<< parameters.airflow_version >>"
          distribution_name: "<< parameters.distribution_name >>"
          extra_args: "<< parameters.extra_args >>"
          image_name: "<< parameters.image_name >>"
  test:
    executor: machine-executor
    description: Test Airflow images
    parameters:
      tag:
        type: string
    steps:
      - airflow-image-test:
          tag: "<< parameters.tag >>"
  scan-trivy:
    docker:
      - image: docker:18.09-git
    description: "Trivy: Vulnerability scanner a Docker image"
    parameters:
      airflow_version:
        description: "The Airflow version, for example '1.10.5'"
        type: string
      distribution_name:
        description: "The base distribution of the container"
        type: string
      distribution:
        description: "The distribution without onbuild"
        type: string
      image_name:
        type: string
        default: "ap-airflow:2.2.0"
    steps:
      - checkout
      - setup_remote_docker
      - attach_workspace:
          at: /tmp/workspace
      - run:
          name: Load archived Docker image
          command: docker load -i /tmp/workspace/saved-images/<< parameters.image_name >>-onbuild.tar
      - run:
          name: Install trivy
          command: |
            apk add --update-cache --upgrade curl rpm
            curl -sfL https://raw.githubusercontent.com/aquasecurity/trivy/master/contrib/install.sh | sh -s -- -b /usr/local/bin
      - restore_cache:
          keys:
            - trivy-cache
      - run:
          name: Scan the local image with trivy
          command: |
            trivy \
              --ignorefile "./<< parameters.airflow_version >>/<< parameters.distribution >>/trivyignore" \
              --cache-dir /tmp/workspace/trivy-cache \
              --ignore-unfixed -s HIGH,CRITICAL \
              --exit-code 1 \
              --no-progress "<< parameters.image_name >>-onbuild"
      - save_cache:
          key: trivy-cache
          paths:
            - /tmp/workspace/trivy-cache
  push:
    executor: docker-executor
    description: Push Airflow images
    parameters:
      dev_build:
        description: "Indicate if this is a dev build"
        type: boolean
      tag:
        type: string
      extra_tags:
        type: string
        default: ""
      extra_tags_file:
        description: ""
        type: string
        default: ""
      prod_docker_repo_docker_hub:
        description: "The docker repo to tag and push to, for example 'quay.io/astronomer/ap-airflow'"
        default: "astronomerinc/ap-airflow"
        type: string
      prod_docker_repo_quay_io:
        description: "The docker repo to tag and push to, for example 'quay.io/astronomer/ap-airflow'"
        default: "quay.io/astronomer/ap-airflow"
        type: string
      dev_docker_repo_quay_io:
        description: "The docker repo to tag and push to, for example 'quay.io/astronomer/ap-airflow'"
        default: "quay.io/astronomer/ap-airflow-dev"
        type: string
    steps:
      - attach_workspace:
          at: /tmp/workspace
      - push:
          dev_release: "<< parameters.dev_build >>"
          extra_tags: "<< parameters.extra_tags >>"
          extra_tags_file: "<< parameters.extra_tags_file >>"
          tag: "<< parameters.tag >>"
          prod_docker_repo_docker_hub: "<< parameters.prod_docker_repo_docker_hub >>"
          prod_docker_repo_quay_io: "<< parameters.prod_docker_repo_quay_io >>"
          dev_docker_repo_quay_io: "<< parameters.dev_docker_repo_quay_io >>"

orbs:
  slack: circleci/slack@4.2.0
  clair-scanner: ovotech/clair-scanner@1.6.0
executors:
  docker-executor:
    docker:
      - image: circleci/python:3
  machine-executor:
    machine:
      image: ubuntu-2004:202010-01

commands:
  docker-build-base-and-onbuild:
    description: "Build Airflow images to use with the Astronomer platform"
    parameters:
      airflow_version:
        type: string
        default: "1.10.5"
      distribution_name:
        type: string
        default: alpine
      extra_args:
        description: "Extra args to pass to pass to Docker build command"
        default: ""
        type: string
      image_name:
        type: string
        default: "ap-airflow:2.2.0"
    steps:
      - checkout
      - setup_remote_docker:
          docker_layer_caching: true
      - docker-build:
          image_name: "<< parameters.image_name >>"
          path: "<< parameters.airflow_version >>/<< parameters.distribution_name >>"
          extra_args: "<< parameters.extra_args >>"
      - docker-build:
          image_name: "<< parameters.image_name >>-onbuild"
          path: "common/"
          dockerfile: "Dockerfile.onbuild-<< parameters.distribution_name >>"
          extra_args: "--build-arg baseimage=<< parameters.image_name >>"
      - persist_to_workspace:
          root: .
          paths:
            - saved-images/
  docker-build:
    description: "Build a Docker image"
    parameters:
      dockerfile:
        type: string
        default: Dockerfile
      path:
        type: string
        default: "."
      image_name:
        type: string
        default: $CIRCLE_PROJECT_REPONAME
      extra_args:
        type: string
        default: ""
    steps:
      - attach_workspace:
          at: /tmp/workspace
      - run:
          name: Build the Docker image
          command: |
            set -xe
            mkdir -p saved-images/"$(dirname '<< parameters.image_name >>')"
            docker build \
              --tag '<< parameters.image_name >>' \
              --label io.astronomer.docker.build_time="$(date +%s)" \
              --label io.astronomer.repo.commit_sha="${CIRCLE_SHA1}" \
              --label io.astronomer.repo.url="${CIRCLE_REPOSITORY_URL}" \
              --label io.astronomer.ci.build_url="${CIRCLE_BUILD_URL}" \
              --file '<< parameters.path>>/<< parameters.dockerfile >>' \
              --build-arg "BUILD_NUMBER=${CIRCLE_BUILD_NUM}" \
              --build-arg "REPO_BRANCH=${CIRCLE_BRANCH}" \
              << parameters.extra_args >> '<< parameters.path >>'
            docker save -o saved-images/<< parameters.image_name >>.tar '<< parameters.image_name >>'
            docker inspect << parameters.image_name >>
  airflow-image-test:
    description: Test an Airflow image
    parameters:
      tag:
        type: string
    steps:
      - checkout
      - attach_workspace:
          at: /tmp/workspace
      - run:
          name: Load archived Airflow Docker image
          command: |
            docker load -i /tmp/workspace/saved-images/ap-airflow:<< parameters.tag >>.tar
            docker load -i /tmp/workspace/saved-images/ap-airflow:<< parameters.tag >>-onbuild.tar
      - run:
          name: Test Airflow Docker images (Base + Onbuild)
          command: |
            set -e
            pyenv global 3.8.5
            pip install -r .circleci/test-requirements.txt
            .circleci/bin/test-airflow 'ap-airflow' '<< parameters.tag >>'
      - store_test_results:
          path: /tmp/test-reports
  push:
    description: "Push a Docker image to DockerHub"
    parameters:
      dev_release:
        description: "Indicate if this is a dev release"
        default: true
        type: boolean
      extra_tags:
        type: string
        default: ""
      extra_tags_file:
        description: "File containing additional tags, one per line"
        type: string
        default: ""
      tag:
        type: string
      image_name:
        default: ap-airflow
        type: string
      prod_docker_repo_docker_hub:
        default: astronomerinc/ap-airflow
        type: string
      prod_docker_repo_quay_io:
        default: quay.io/astronomer/ap-airflow
        type: string
      dev_docker_repo_quay_io:
        default: "quay.io/astronomer/ap-airflow-dev"
        type: string
    steps:
      - attach_workspace:
          at: /tmp/workspace
      - setup_remote_docker
      - run:
          name: Load archived Docker image
          command: docker load -i '/tmp/workspace/saved-images/ap-airflow:<< parameters.tag >>.tar'
      - run:
          name: Login to DockerHub
          command: echo "$DOCKER_PASSWORD" | docker login --username "$DOCKER_USERNAME" --password-stdin docker.io
      - run:
          name: Login to Quay.io
          command: echo "$QUAY_PASSWORD" | docker login --username "$QUAY_USERNAME" --password-stdin quay.io
      - run:
          name: Push Docker image(s)
          command: |
            set -e
            image="ap-airflow:<< parameters.tag >>"
            set -x
            export NEW_POINT_RELEASE=true

            IFS="," read -ra DOCKER_TAGS \<<< "<< parameters.extra_tags >>"

            echo "DOCKER_TAGS: ${DOCKER_TAGS[@]}"

            # Read in more tags from the extra_tags_file, if it exists
            if [[ -f "<< parameters.extra_tags_file >>" ]]; then
              echo "Contents of << parameters.extra_tags_file >>:"
              cat "<< parameters.extra_tags_file >>"

              while read tag;
              do
                DOCKER_TAGS+=($tag)
              done < "<< parameters.extra_tags_file >>"

              echo "DOCKER_TAGS:"
              echo "${DOCKER_TAGS[@]}"
            fi

            for tag in "${DOCKER_TAGS[@]}";
            do
              if DOCKER_CLI_EXPERIMENTAL=enabled docker manifest inspect "<< parameters.prod_docker_repo_docker_hub >>:$tag" >/dev/null 2>&1; then
                echo "Image with Tag ("<< parameters.prod_docker_repo_docker_hub >>:$tag") already exists"
                export NEW_POINT_RELEASE=false
              elif [[ "$tag" == "<< parameters.tag >>-$CIRCLE_BUILD_NUM" ]] || <<parameters.dev_release >> ; then
                echo "Tagging and pushing image <<parameters.image_name>>:$tag to << parameters.dev_docker_repo_quay_io >>"
                docker tag "$image" "<< parameters.dev_docker_repo_quay_io >>:${tag}"
                docker push "<< parameters.dev_docker_repo_quay_io >>:${tag}"

              else
                echo "Tagging and pushing image <<parameters.image_name>>:$tag to << parameters.prod_docker_repo_quay_io >>"
                docker tag "$image" "<< parameters.prod_docker_repo_quay_io >>:${tag}"
                docker push "<< parameters.prod_docker_repo_quay_io >>:${tag}"

                echo "Tagging and pushing image <<parameters.image_name>>:$tag to << parameters.prod_docker_repo_docker_hub >>"
                docker tag "$image" "<< parameters.prod_docker_repo_docker_hub >>:${tag}"
                docker push "<< parameters.prod_docker_repo_docker_hub >>:${tag}"

                echo "Tagging and pushing image <<parameters.image_name>>:$tag to << parameters.dev_docker_repo_quay_io >>"
                docker tag "$image" "<< parameters.dev_docker_repo_quay_io >>:${tag}"
                docker push "<< parameters.dev_docker_repo_quay_io >>:${tag}"

              fi
            done

            # The following code block publishes Moving Master builds (e.g. 1.10.13-buster-onbuild)
            # as compared to above code-blocks that publish images with build-number & immutable tag
            # e.g (e.g. 1.10.13-1-buster-onbuild and 1.10.13-buster-onbuild-24119)
            if $NEW_POINT_RELEASE ; then
              if ! <<parameters.dev_release >> ; then
                # If it is not a Dev Release publish the image to Prod Repos
                echo "Tagging and pushing image <<parameters.image_name>>:<< parameters.tag >> to << parameters.prod_docker_repo_quay_io >>"
                docker tag "$image" "<< parameters.prod_docker_repo_quay_io >>:<< parameters.tag >>"
                docker push "<< parameters.prod_docker_repo_quay_io >>:<< parameters.tag >>"

                echo "Tagging and pushing image <<parameters.image_name>>:<< parameters.tag >> to << parameters.prod_docker_repo_docker_hub >>"
                docker tag "$image" "<< parameters.prod_docker_repo_docker_hub >>:<< parameters.tag >>"
                docker push "<< parameters.prod_docker_repo_docker_hub >>:<< parameters.tag >>"
              fi

              echo "Tagging and pushing image <<parameters.image_name>>:<< parameters.tag >> to << parameters.dev_docker_repo_quay_io >>"
              docker tag "$image" "<< parameters.dev_docker_repo_quay_io >>:<< parameters.tag >>"
              docker push "<< parameters.dev_docker_repo_quay_io >>:<< parameters.tag >>"
            else
              echo "Image with Tag ($image) not pushed as it is not a new point release"
            fi
            set +x
